import 'package:flutter/material.dart';

class ContainerZero extends StatelessWidget {
  const ContainerZero();

  @override
  Widget build(BuildContext context) {
    return Container(width: 0, height: 0);
  }
}
